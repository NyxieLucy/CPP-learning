### over all

basically i'm trying to learn the basics of C++ now

### how

by making small mini projects

### projects

## 1. basic calculator

i know classic basic stuff, but one must get familiar with the syntax so yeah
in this one, i made notes bout some data types, and if else statements  and basic math operators
also there is data output and input like `std::cout << "hello world` and for the input `std::cin << int variable`
so that's that basic stuff but it serves a purpose

## 2. basic Tamagotchi game

here i cover the OOP in a shallow matter, principles like encabsulation
also constructors and brief functions and all, or precisely methodes
and yeah we used switches instead of if else statements and a while loop
not much but it's honest work

## 3. arrays (registery simple system)

lowkey fuck pointers, that's first of all,
second of all here we learn pointers kinda, i still don't know what the hell i'm doing,
just compiling and checking errors trying stuff until it works,
but what i did understand in this example is that, we basically rent a place in memory assign it values in stuff with the vector we created, then if it overloaded it expands deleting what was there before to create more space for more content,
and pointers are the stuff that snitches on the memory address in that sense, they be like "this way officer the weed is at this location in the memory"
anyways, the syntax i suppose is just like rust over all:

```
int x = 3;
int *p = &x ;
std::cout <<  *p;
```

like they be looking at it, and i hate it honestly, but alright, i'll learn about it more, but for now
fuck them pointers
